/// Cœur partagé Éco-Responsable.
///
/// Les applications (`mobile/`, `desktop/`, `web/`) n'ont besoin que de
/// [runEcoApp] et [AppTarget] ; le reste est accessible via les imports
/// `package:eco_core/...` pour les tests et les extensions.
library;

export 'app.dart';
export 'bootstrap.dart';
export 'core/app/app_target.dart';
